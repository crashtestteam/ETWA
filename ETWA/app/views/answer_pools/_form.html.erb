<%= form_for(@answer_pool) do |f| %>
  <% if @answer_pool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_pool.errors.count, "error") %> prohibited this answer_pool from being saved:</h2>

      <ul>
      <% @answer_pool.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :points %><br>
    <%= f.number_field :points %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :correct %><br>
    <%= f.check_box :correct %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
