<%= form_for(@labels_question) do |f| %>
  <% if @labels_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@labels_question.errors.count, "error") %> prohibited this labels_question from being saved:</h2>

      <ul>
      <% @labels_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :label_id %><br>
    <%= f.number_field :label_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
