<%= form_for(@attempt_answer) do |f| %>
  <% if @attempt_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attempt_answer.errors.count, "error") %> prohibited this attempt_answer from being saved:</h2>

      <ul>
      <% @attempt_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer_id %><br>
    <%= f.number_field :answer_id %>
  </div>
  <div class="field">
    <%= f.label :attempt_id %><br>
    <%= f.number_field :attempt_id %>
  </div>
  <div class="field">
    <%= f.label :chosen %><br>
    <%= f.check_box :chosen %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
